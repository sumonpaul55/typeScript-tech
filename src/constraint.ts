{
    // constraints
    // constraints is <T extandes {id: number, name: string}> // it means id and name must be included

    // T must be received {id:number, name:string}


    // it use for ensure some dynamic type like id and anme
}